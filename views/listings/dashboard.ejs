<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes</title>
    <link rel="stylesheet" href="../../style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@200..900&display=swap" rel="stylesheet">
</head>
 <style>
  .temp{
    height: 190px;
    overflow: hidden;
    text-overflow: ellipsis;
  }
 </style>
<body>
    <header class="d-flex flex-wrap justify-content-center py-3 px-5 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
          <span class="fs-4">Google Notes</span>
        </a>
      
        <div class="search">
            <form class="d-flex" role="search" method="POST" action="/dashboard/search">
              <input class="search-box" name="searchTerm" type="search" placeholder="Search Notes By Username..." aria-label="Search">
              <button class="btn search-btn" type="submit">Q</button>
            </form>
        </div>
      
        <div class="col-md-3 text-end">
          <a href="/" type="button" class="btn btn-outline-dark me-2">Home</a>
          <a href="/logout" type="button" class="btn btn-primary">Log Out</a>
        </div>
      </header>

    <main class="spe-font px-2">
      <div class="container-fluid container-fluid-custem pb-5 mb-5">
        <div class="row py-4 px-4">
            <div class="col">
                <h1>Hey, <%= user %></h1>
            </div>
            <div class="col text-end">
                <a href="dashboard/add" class="btn btn-primary">Create Note</a>
            </div>
        </div>

        <div class="row px-5">
        <% if(allNotes.length != 0) { %>
        <%  for(note of allNotes) { %>
          <div class="main-card card border-3 border-primary rounded mb-3" style="width: 20rem; display: flex; flex-wrap: wrap;">
            <div class="card-body main-card bg-light">
              <h6 class="card-title bg-light"><%= note.title %></h6>
              <p class="card-text bg-light temp"><%= note.body %></p>
            </div>
            <a href="/dashboard/item/<%=note._id%>" class="bg-light text-end px-3" style="background-color: white;">More</a>
          </div>&nbsp;&nbsp;&nbsp;&nbsp;
        <%  } %>
        <% } else { %>
          <div class="row">
            <div class="col-3">
              <img src="https://raw.githubusercontent.com/RaddyTheBrand/Notes-NodeJs-CRUD-MongoDB/8035f76e7b9a106c981fde8ef32dc3cf363260bf/public/img/human-3.svg" alt="No Notes Found !">
            </div>
            <div class="col mtmd-4">
              <h4>Let's Start With Your First Note..</h4>
              <a href="dashboard/add">Create Note</a>
            </div>
          </div>
        <% } %>
        </div>
    </div>
    </main>

    <%- include("../includes/footer.ejs") %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>